info:
  name: DELETE /messages/:id
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/messages/{{messageId}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
