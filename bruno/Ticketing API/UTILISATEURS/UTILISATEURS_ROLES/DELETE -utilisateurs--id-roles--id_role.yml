info:
  name: DELETE /utilisateurs/:id/roles/:id_role
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/utilisateurs/{{userId}}/roles/{{roleId}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
