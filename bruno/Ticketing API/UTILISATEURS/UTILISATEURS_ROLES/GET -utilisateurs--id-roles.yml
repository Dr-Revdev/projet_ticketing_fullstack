info:
  name: GET /utilisateurs/:id/roles
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/utilisateurs/{{userId}}/roles"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
