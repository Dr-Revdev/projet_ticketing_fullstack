info:
  name: PATCH /categories/:id
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/categories/{{categorieId}}"
  body:
    type: json
    data: |-
      {
        "libelle": "Catégorie modifiée Bruno"
      }
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
