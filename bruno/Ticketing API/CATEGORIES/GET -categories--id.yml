info:
  name: GET /categories/:id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/categories/{{categorieId}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
