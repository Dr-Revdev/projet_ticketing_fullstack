info:
  name: GET /historique-actions/:id
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/historique-actions/{{historiqueId}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
