info:
  name: GET /historique-actions
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/historique-actions"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
