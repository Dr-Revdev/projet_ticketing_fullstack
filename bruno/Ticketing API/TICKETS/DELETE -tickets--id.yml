info:
  name: DELETE /tickets/:id
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/tickets/{{ticketId}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
