info:
  name: DELETE /equipes/:id
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/equipes/{{equipeId}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
