info:
  name: DELETE /piece-jointes/:id
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/piece-jointes/{{pieceJointeId}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
