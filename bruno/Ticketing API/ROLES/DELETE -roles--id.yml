info:
  name: DELETE /roles/:id
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/roles/{{roleId}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
