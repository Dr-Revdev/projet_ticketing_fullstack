info:
  name: PATCH /roles/:id
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/roles/{{roleId}}"
  body:
    type: json
    data: |-
      {
        "libelle": "admin"
      }
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
