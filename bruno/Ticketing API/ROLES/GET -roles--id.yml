info:
  name: GET /roles/:id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/roles/{{roleId}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
